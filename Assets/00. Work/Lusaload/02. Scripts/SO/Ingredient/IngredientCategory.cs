namespace _00._Work.Lusaload._02._Scripts.SO
{
    // 재료의 종류를 구분하는 열거형
    public enum IngredientCategory
    {
        BaseAlcohol, // 기주 (진, 럼, 보드카 등)
        Drink,       // 음료 (주스, 소다수 등)
        Garnish      // 가니시 및 부재료 (시럽, 소금 등)
    }
}
